<UserControl x:Class="C490_App.Components.ScrollMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:C490_App.Components"
             xmlns:viewmodel="clr-namespace:C490_App.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="400">
    <Grid Background="White">
        <ListView >
            <Expander Header="LED Parameter" Content="{Binding ExperimentLocal.ledParameters}" Width="259"/>
            <Expander Header="Select Experiment" Content="hey" Width="259"/>
            <Expander Header="Set Experiment Parameters" Content="hey" Width="259"/>
        </ListView>

    </Grid>
</UserControl>
